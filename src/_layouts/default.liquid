<!doctype html>
<html lang="en">
  <head>
    {% render "head", metadata: site.metadata, title: page.title %}
  </head>
  <body class="{{ page.layout }} {{ page.page_class }}">
    <main data-controller="header-link">
      {{ content }}
    </main>

    {% render "nav_button" %}

    {% render "footer" %}
  </body>
</html>
